<h1 class="mt-4">Prendre rendez-vous</h1>

<%# <div class="pt-24"> %>
<div class="col-6 offset-3">
  <div class="form-basic mt-5">
    <%= simple_form_for [@user, @appointment] do |f| %>
      <%= f.input :date, as: :date, html5: true %>
      <%= f.input :time, as: :time, html5: true %>
      <p>Tarif: 50€</p>
      <%= f.button :submit, "Confirmer", class: "btn btn-basic btn-confirm" %>
    <% end %>
  </div>
</div>
